# removeDupProtSeqs

Removing duplicated proteins sequences in a fast file. 
```
docker pull mengchen18/remove_dup_protseqs
```
Example how to start:
```
docker run -d --rm -v /local/dir/:/home/shiny/projects -p 9999:3838 mengchen18/remove_dup_protseqs
```
